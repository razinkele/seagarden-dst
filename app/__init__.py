"""SeaGarden DST Shiny application.

Imported as a package from the repository root (`shiny run app.app`), so that
`from app.modules... import` resolves. The application depends on `seagarden_dst`;
the core never imports anything from here.
"""
